# CLAUDE.md

See [AGENTS.md](AGENTS.md). Same instructions, and it is the file every agent in
this repo reads.

Two shortcuts worth having in context:

- Validate before committing: `python3 scripts/validate.py`
- Sync the plugin copy after editing skills: `./scripts/sync-plugin.sh`
